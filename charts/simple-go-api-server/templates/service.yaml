apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-go-api-server.fullname" . }}
  labels:
    {{- include "simple-go-api-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-go-api-server.selectorLabels" . | nindent 4 }}
